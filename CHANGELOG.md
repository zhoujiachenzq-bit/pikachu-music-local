# 更新日志

## v0.4.0-beta.23 — 2026-08-29

- 引入 LangGraph 音乐意图影子图，按“本地解析 → 站内证据 → 模型分类 → Critic 反思”审计易歧义点歌请求。
- 影子推断不接管播放器和回复；新增脱敏审计表，记录旧逻辑与反思结论的差异，为下一阶段切换提供评测数据。
- 歌名或歌手证据冲突时强制标记为需要用户选择；模型结论与明确站内证据冲突时由确定性 Critic 修正。

## v0.3.2 — 2026-08-20

- 修复 `go-music-api` 在部分数据中心中 `/switch` 返回弱候选、但完整 `/search` 已存在准确原唱时未继续回退的问题。
- 快捷切换无可信结果时，按歌名、歌手和时长对完整搜索结果重新排序，并逐项执行真实音频探测；酷我限制提示音仍会被拦截。
- 香港服务器实测 QQ《退后》备用搜索可定位酷我原唱 ID `138246`、261 秒，不降低衍生版本和 8 秒时长容差规则。

## v0.3.1 — 2026-08-20

- QQ 搜索改用官方公开元数据接口，保留平台歌曲 ID，并补齐专辑、封面和精确时长。
- 旧 QQ、网易云和咪咕记录在解析前自动校准缺失时长；只有同名、同歌手且时长误差不超过 8 秒的原唱候选才允许自动跨源播放。
- `go-music-api` 增加同平台 ID 验证、备用搜索和真实音频探测；咪咕真实复合 ID 可直接使用同源备用流，旧元数据 ID 使用多源时长证据后再回退。
- 调整解析顺序，QQ 公开播放地址失效时优先使用已验证备用链路，避免等待不稳定旧接口超时；网易云、酷我和咪咕仍保持独立故障隔离。
- 播放进度条在拖动期间使用独立草稿值，松手后才提交定位，避免被播放器同步时钟拉回；音量条只在松手后持久化，消除异步写入回跳。
- 重绘跨浏览器进度与音量滑条，补齐触控命中区、禁用状态、填充进度和键盘焦点反馈。
- 修复从手机宽度切换到桌面宽度后播放器保留移动端滚动位置、导致中栏内容整体错位的问题。
- 增强本地服务自动恢复、音频兼容代理、来源熔断恢复、推荐版本去重和原唱优先规则。

## v0.3.0 — 2026-08-19

- 重构为沉浸式三栏视觉系统，加入六套账户级本地主题与 55% 封面氛围色融合。
- 增加皮卡丘夜幕、午夜唱片店、霓虹游戏厅三种中栏场景，并为手机与桌面提供一致的动态效果和 CSS 降级。
- 每日推荐改为独立中栏舞台；手机端增加推荐、搜索、播放、歌单四栏目导航和迷你播放器。
- 保持单一音频元素、播放队列、歌词定位、Media Session、PWA 与既有 SQLite 数据完全兼容。

## v0.2.1（草稿）— 2026-08-14

- 保持开放注册体验，新增同 IP 每日注册和全站每小时注册配额；正常用户无需邀请码或验证码。
- 登录限速拆分为账户与 IP 两层，成功登录仅解除账户桶，避免单一攻击者锁死所有用户或轮换用户名绕过限制。
- 搜索、播放解析、导入、推荐生成、播放记录和备份增加按账户/IP划分的资源配额与标准重试提示。
- 新账户首日降低导入额度，同时限制单账户并发导入、歌单数量、单歌单歌曲数和行为记录总量。
- 增加可信反向代理配置，拒绝不安全的 `TRUST_PROXY=true`；同源校验不再信任客户端可伪造的 `X-Forwarded-Host`。
- 增加 CSP、HSTS、点击劫持防护、MIME 嗅探防护、权限策略和 API `no-store` 等安全响应头。
- 备份恢复改用有数量和长度上限的嵌套结构校验；第三方接口响应增加流式体积和读取超时限制。
- 酷我搜索及公开歌单接口切换为 HTTPS；容器增加健康检查，Service Worker 缓存版本更新至 v0.2.1。
- 将构建链中的 `nanoid` 固定到已修复版本，生产与开发依赖审计均无已知漏洞。
- 修复 Windows PowerShell 5 对无 BOM UTF-8 启动脚本的解析问题，桌面快捷方式可继续启动正式构建与备用音源。

## v0.2.0 — 2026-08-13

- 新增每日 30 首推荐，根据收藏、歌单和实际播放完成度生成熟悉曲目与探索曲目，并保留最近 7 天历史。
- 增加“不感兴趣”和“减少该歌手”反馈；歌曲使用规范化歌名与歌手去重，不合并不同平台的原始收藏记录。
- 记录播放完成、跳过、错误和实际音源，为后续推荐提供本地学习数据，所有记录继续按账户隔离。
- 新增来源健康监测和熔断保护，限制短期接口缓存规模；完善登录限速、导入任务恢复、同源串行和仅重试失败项。
- 修复酷我搜索接口变化，并识别、拦截“只能在酷我播放”的短提示音；高可信匹配会自动切换其他音乐源。
- 可选接入 `go-music-api v1.0.1` 作为最后一级备用解析服务，增加严格二次匹配、音频探测、同源 Range 流代理和单次重试。
- Windows 安装脚本会校验 Go 工具链，固定第三方版本，并编译只监听 `127.0.0.1:8080` 的本地 sidecar；后备服务故障不会影响原有四源和本地数据。
- 更新 Service Worker 至 v0.2.0，仅缓存页面外壳与静态资源，不缓存 API、歌词或音频。

## v0.1.3 — 2026-08-03

- 支持 Android Chrome 切换应用、熄屏与锁屏后台播放，并加入通知栏/锁屏媒体控制。
- 增加 PWA Manifest、安装入口、图标和仅缓存页面外壳的 Service Worker。
- 修复手机进入页面自动跳转、歌词停滞、整页被歌词定位带动以及手动浏览被强制拉回的问题。
- 重做列表循环、原生单曲循环和无重复随机队列；播放队列固定为显式点歌时的列表快照。
- 增加随机播放历史返回、失败歌曲跳过和基于固定队列的下一首预加载。
- 歌词点击优先使用精确时间轴；当前来源只有纯文本时自动匹配其他音乐源的 LRC。
- 跨源仍找不到时间轴时，从第一句正文歌词开始估算，排除歌名、制作人员和版权声明。
- 手机页面位置按本地账户保存，返回前台时从真实音频状态校准进度与歌词。

## v0.1 — 2026-08-01

- 完成三栏霓虹音乐播放器及桌面、平板、手机响应式布局。
- 支持咪咕、网易云、QQ、酷我搜索、播放、歌词与公开歌单导入。
- 增加本地账户、收藏、自建歌单、同步、排序和 JSON 备份恢复。
- 增加跨源播放回退、短期解析缓存和失败提示。
- 增加皮卡丘动画、歌词扫光、全界面点击水波及歌词时间定位。
- 数据保存至本机 SQLite，服务仅监听 `127.0.0.1`。
